# Optional. Poe's /v1/models endpoint is public and works without a key.
# If set, the proxy sends it as Bearer auth on model list requests.
# Get a key from: https://poe.com/api_key
# POE_API_KEY=

# Optional listen port (default 8787)
# PORT=8787
