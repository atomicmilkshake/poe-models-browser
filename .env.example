# Poe API key — used by the local proxy (proxy.js) to forward
# authenticated requests to https://api.poe.com/v1/models.
# NEVER commit your real key. This file is gitignored.
# Get a key from: https://poe.com/api_key
POE_API_KEY=
