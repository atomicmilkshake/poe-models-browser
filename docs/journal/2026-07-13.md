# Journal — 2026-07-13

## 10:30 - Launch.bat + version 1.1.0

- **Did:** Fixed Windows launcher to detach `electron.exe` (no stuck console). Bumped `package.json` / lock to **1.1.0** so npm no longer prints stale 1.0.0.
- **Why:** Double-click launch looked idle; console showed outdated version after README-only v1.1.0 bump.
- **Next:** Commit when user wants; optional UI version label.

## 11:15 - Costanza frontier + optional Pareto overlay

- **Did:** Implemented Costanza frontier (intelligence P70–P85 hard ceiling, in-band Pareto). Chart overlays: Pareto (default on) and Costanza (default off), distinct colors, legend. Styles in `styles.css`; toggles in chart panel.
- **Why:** User coined Costanza as thrifty upper-mid intelligence band; requested hard ceiling at 85th and optional display.
- **Next:** Optional percentile knobs in UI.

## 12:20 - Reasoning filter chip

- **Did:** Mapped Poe `reasoning` object → `supports_reasoning`; sidebar **Reasoning** chip filters to non-null reasoning models.
- **Why:** Data existed on Poe API but was unused.
- **Next:** Optional non-reasoning chip if needed.

## 12:40 - Product research notes (not shipped)

- **Did:** Documented Copilot custom-endpoint compatibility discussion (verified ≈0 public Poe proofs; likely ≈63 by tools+chat+128k). Confirmed `thinking:false` is **not** the model-picker gate (`toolCalling` is). Captured local proxy payload insights path under product-notes.
- **Why:** User explored filters; deferred shipping Copilot filter.
- **Next:** Only ship with soft wording + evidence if revisited.

## 12:46 - Docs hub, bootstrap, journal enforcement

- **Did:** Added `AGENTS.md`, `docs/` hub (index, architecture, product-notes, journal README), `scripts/bootstrap.mjs`, `scripts/journal-check.mjs`, `scripts/lib/journal.mjs`. Wired npm scripts. Seeded this journal.
- **Why:** Centralize docs; prevent agent amnesia; enforce per-turn dev journal.
- **Next:** Future agents run `npm run bootstrap` first; append here every write-turn.

## 12:50 - Commit, push, and ship v1.2.0

- **Did:** Bumped to **1.2.0**; encoded global rule “commit ⇒ update GitHub Release” in `~/.grok/AGENTS.md` and project `AGENTS.md`. Commit + push all pending work; create GitHub release `v1.2.0` as Latest.
- **Why:** User asked to commit/push everything, keep GitHub release current, and apply commit=release on any project from now on.
- **Next:** None for this batch.
