{
  "name": "poe-models-browser",
  "version": "1.0.0",
  "private": true,
  "description": "Browse, filter, and benchmark Poe /v1/models catalog",
  "scripts": {
    "start": "node proxy.js",
    "serve": "node proxy.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
